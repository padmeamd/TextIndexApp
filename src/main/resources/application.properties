spring.application.name=testApp

# Elasticsearch connection — overridden in Kubernetes via SPRING_ELASTICSEARCH_URIS env var
spring.elasticsearch.uris=${SPRING_ELASTICSEARCH_URIS:http://localhost:9200}
